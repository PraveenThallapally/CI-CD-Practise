apiVersion: apps/v1
kind: Deployment
metadata:
  name: praveen-devops-page
  namespace: qa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: praveen-devops-page
  template:
    metadata:
      labels:
        app: praveen-devops-page
    spec:
      containers:
      - name: praveen-devops-page
        image: praveen1351/devops-page:
        ports:
        - containerPort: 80
